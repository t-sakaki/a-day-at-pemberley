# RevenueCat Web Billing — public API key for @revenuecat/purchases-js.
# Dashboard → Project settings → API keys → "Web Billing" (public) key.
# Without this, "Pemberley Pro" is hidden and the app runs fully for free.
VITE_REVENUECAT_WEB_API_KEY=
