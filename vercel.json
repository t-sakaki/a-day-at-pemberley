{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "BASE_PATH=/ PORT=5173 pnpm --filter @workspace/pemberley-day run build",
  "outputDirectory": "artifacts/pemberley-day/dist/public",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
